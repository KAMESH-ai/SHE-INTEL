"""
Root-level pytest configuration.

pytest automatically adds the directory containing this file to sys.path,
so ``backend`` is importable as a top-level package when tests are run
from the project root with ``pytest backend/tests``.
"""
